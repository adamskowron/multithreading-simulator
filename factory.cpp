#include "factory.h"

Factory::Factory(int W, int M)
{
	//createWorkers(i);
	//GenerateItems(i*4);
	//thread* t = new std::thread();
	//std::thread t(&Factory::run,&p);
}

void Factory::run()
{
	int i = 0;
}

